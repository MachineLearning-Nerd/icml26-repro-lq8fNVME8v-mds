# Evaluation contract

The verdict is FALSIFIED only if all four printed assumptions, exact regular
conditionals, correct specification, argmin existence, and original almost
sure consistency are certified; the MDS objective must strictly select the
escaping posterior; a bounded continuous test function must refute weak
convergence; and the independent checker must pass. The verifier exits nonzero
for the Gaussian-kernel negative control and for any failed condition.
