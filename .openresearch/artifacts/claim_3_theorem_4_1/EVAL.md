# Evaluation contract

Current result is generated by the fixed experiment command. The claim is
FALSIFIED only if all ten printed assumptions pass, the summary rate is 1/3,
the KL/epsilon rate is -1/3, an independent checker confirms both constants,
and the nondegenerate negative control exits nonzero. Any failed condition
makes the verifier exit nonzero.
