# Limitations and deviations

This is direct implementation evidence on two paper mechanisms, not a
universal proof about all possible NPE architectures. The official adapter
path is pinned as a submodule, and the NPE tensor equality check is exact.
