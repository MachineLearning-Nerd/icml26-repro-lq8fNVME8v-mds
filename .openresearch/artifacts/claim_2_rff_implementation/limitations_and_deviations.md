# Limitations and deviations

The audit directly executes the paper-scale 512-feature implementation on
Gaussian and OUP. Timings are hardware-specific. The epsilon 0.5 optimizer
boundary is retained as a limitation and negative control.
